# reports/

Generated by `scripts/build_radar.py`. Each run writes a timestamped
Markdown report (e.g. `radar-2026-08-31.md`) built only from studies in
`research/verified/` that pass deterministic validation, are `GREEN`, and
fall inside the active research window. Reports are never hand-edited —
regenerate them by re-running the builder.
